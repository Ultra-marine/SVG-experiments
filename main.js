$(function(){	var list = $("#list p")	$('<svg class="svg"></svg>').insertAfter(list)		var 		svg = $("#list .svg"),		sh = list.html(),		l = []				$.each(svg,function(){			s = Snap("svg")			s.attr({				viewBox : "-5,-5,10,10"			})					})				$.each(s,function(){			s.paper.circle(0,0,5)					})					$("#dis").text(svg[1])/*		s.attr({			viewBox : "0,0,10,10"		})			s.paper.circle(0,0,5)*/															});